{
  "tasks": {
    "dev": "deno run --watch --allow-net --allow-env --env-file --allow-read --unstable-kv src/server.ts",
    "poll": "deno run --allow-net --allow-env --env-file --allow-read src/poller/index.ts",
    "check": "deno check src/",
    "fmt": "deno fmt"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@std/dotenv": "jsr:@std/dotenv@^0.225",
    "@std/encoding": "jsr:@std/encoding@1",
    "imagescript": "jsr:@matmen/imagescript@1"
  },
  "fmt": {
    "exclude": ["README.md"]
  },
  "unstable": ["kv"],
  "deploy": {
    "org": "alextibtab",
    "app": "now-playing",
    "runtime": {
      "mode": "dynamic",
      "entrypoint": "./src/server.ts"
    }
  }
}
